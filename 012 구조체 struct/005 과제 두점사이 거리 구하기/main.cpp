/*
	과제 ] 두점사이 거리구하기

	struct POINT { x, y }

	거리 = calcDistance (&p1, &p2);

	POINT tower
	POINT enemy
	BOOL = tower. isinRange ( &enemy ) ;
	-> true "발싸!"
	-> false "대기"


	[[ 두점사이 거리공식 활용 ]]

	#include <math.h>
	pow( ,n) -> n제곱승
	sqrt() -> 제곱근 (루트 연산)
*/